import React from 'react'
import {useParams} from 'react-router-dom'

const CampaignDetailsPage = () => {

    const {id} = useParams()

    //Get campaign data for campaign #9
    //verify that current user is the DM of the campaign in question
    //create a form for adding users to the campaign

    return (
        <div>
            
        </div>
    )
}

export default CampaignDetailsPage
